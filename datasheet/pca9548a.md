# PCA9548A Firmware Implementation Specification

## 2026-09-08 Driver Audit

Rechecked the PDF control register, STOP activation, address table, reset and
I2C timing. The PDF remains authoritative; this is an engineering reference,
not a replacement for package drawings, electrical qualification or errata.

`unit_pahub_mask_set()` accepts `0x00..0x3F`; zero disables every board port.
`unit_pahub_mask_get()` returns the entire hardware byte for diagnostics.
`channel_get()` returns `0xFF` for zero, multi-channel or non-board masks.
Automatic downstream reads/writes select exactly one port under the hub mutex.
Readback updates cached routing; transfer failures invalidate it. A later
transaction reselects the channel after an error.

Retries cannot recover a bus physically held low. Reinitialize after external
power/reset events before trusting cached state. Initialize/deinitialize only
while downstream clients are stopped. Failed disable/remove operations retain
ownership for a later deinit retry. Hardware qualification remains necessary.

## Source Evidence

Converted and verified from checked-in `pca9548a.pdf`, Texas Instruments PCA9548A revision G (March 2021), SHA-256 `fb203781fbac2d5de118ad64b15f6a6a7c93eb1121a2d00af3e6daa7f555d5b7`. Current manufacturer context is the official [TI PCA9548A product page](https://www.ti.com/product/PCA9548A). M5Stack board-level restrictions are documented separately in `pahub.md` and `schema.yml`.

## 1. Purpose

This document is a **self-contained firmware implementation reference** for the **PCA9548A 8-Channel I²C Switch**.

The purpose of this specification is to provide **all information required to implement a complete driver** for the device without needing any additional documentation.

This specification enables:

* Firmware driver generation
* Hardware abstraction layer (HAL) implementation
* Board support package (BSP) integration
* Protocol stack integration
* Automated firmware generation
* Hardware integration validation
* Test harness generation

All numeric parameters, register definitions, electrical limits, timing requirements, and communication procedures required to implement a correct driver are included.

---

# 2. Device Overview

## 2.1 Device Function

The **PCA9548A** is an **8-channel bidirectional I²C switch** that allows a single I²C master to communicate with multiple downstream I²C buses.

The device routes the upstream I²C bus (SDA/SCL) to one or more of eight downstream channels:

```
SC0/SD0
SC1/SD1
SC2/SD2
SC3/SD3
SC4/SD4
SC5/SD5
SC6/SD6
SC7/SD7
```

Each channel can be individually enabled or multiple channels may be enabled simultaneously.

---

## 2.2 Intended Use

Typical applications:

* Resolving I²C slave address conflicts
* Multiplexing multiple identical sensors
* Distributing I²C bus capacitance
* Voltage translation between I²C buses
* System management bus (SMBus) switching

---

## 2.3 Key Capabilities

* 8-channel bidirectional I²C switch
* I²C Standard Mode (100 kHz)
* I²C Fast Mode (400 kHz)
* Voltage translation between 1.8 V, 2.5 V, 3.3 V, and 5 V buses (via the pass-gate and external pull-ups)
* 5 V-tolerant inputs
* Active-low RESET input (recovers a stuck downstream bus without removing power)
* Three hardware address pins (A0, A1, A2) → up to 8 devices on one bus
* Channel selection through the I²C bus (single 8-bit control register)
* All channels deselected on power-up (glitch-free power-up)
* Supports hot insertion
* Low ON resistance pass-gate switching
* Latch-up > 100 mA per JESD 78 Class II
* Operating temperature −40 °C to 85 °C

### Package Options

| Package | Drawing | Pins |
| ------- | ------- | ---- |
| SSOP    | DB      | 24   |
| TVSOP   | DGV     | 24   |
| SOIC    | DW      | 24   |
| TSSOP   | PW      | 24   |
| VQFN    | RGE     | 24   |

Pin numbering in this document is for the **DB/DGV/DW/PW** 24-pin packages. The VQFN (RGE) package uses a different pin order; refer to the datasheet for that variant. Pin assignment does **not** affect the I²C programming model.

---

## 2.4 Internal Subsystems

Internal blocks include:

* I²C bus control logic
* Input filtering
* Reset circuit
* Switch control logic
* 8 bidirectional pass-gate switches

---

# 3. Electrical Specifications

## 3.1 Supply Voltage

| Parameter | Min | Max | Unit |
| --------- | --- | --- | ---- |
| VCC       | 2.3 | 5.5 | V    |

---

## 3.2 Input Voltage Thresholds

### High Level Input Voltage (VIH)

| Signal       | Min       | Max         |
| ------------ | --------- | ----------- |
| SCL, SDA     | 0.7 × VCC | 6 V         |
| A2–A0, RESET | 0.7 × VCC | VCC + 0.5 V |

---

### Low Level Input Voltage (VIL)

| Signal       | Min    | Max       |
| ------------ | ------ | --------- |
| SCL, SDA     | -0.5 V | 0.3 × VCC |
| A2–A0, RESET | -0.5 V | 0.3 × VCC |

---

## 3.3 Operating Temperature

| Parameter             | Min   | Max  |
| --------------------- | ----- | ---- |
| Operating temperature | -40°C | 85°C |

---

## 3.4 Input Leakage

| Parameter     | Min   | Max   |
| ------------- | ----- | ----- |
| Input leakage | -1 µA | +1 µA |

---

## 3.5 Supply Current

### Operating Mode (f_SCL = 400 kHz)

| VCC   | Typ   | Max   |
| ----- | ----- | ----- |
| 5.5 V | 50 µA | 80 µA |
| 3.6 V | 20 µA | 35 µA |
| 2.7 V | 11 µA | 20 µA |

### Operating Mode (f_SCL = 100 kHz)

| VCC   | Typ  | Max   |
| ----- | ---- | ----- |
| 5.5 V | 9 µA | 30 µA |
| 3.6 V | 6 µA | 15 µA |
| 2.7 V | 4 µA | 8 µA  |

### Standby Mode

| VCC   | Typ    | Max  |
| ----- | ------ | ---- |
| 5.5 V | 0.2 µA | 2 µA |
| 3.6 V | 0.1 µA | 2 µA |
| 2.7 V | 0.1 µA | 1 µA |

Standby values apply for both all-low and all-high input conditions.

---

## 3.6 Power-On Reset (POR) Voltage

| Parameter | Description                          | Min   | Typ  | Max   | Unit |
| --------- | ------------------------------------ | ----- | ---- | ----- | ---- |
| VPORR     | POR trip point, VCC rising           | 1.033 | 1.2  | 1.428 | V    |
| VPORF     | POR trip point, VCC falling          | 0.767 | 1.0  | 1.144 | V    |

To force a full power-on reset, VCC must drop **below VPORF** and then ramp back above VPORR. Brief sags that stay above VPORF do **not** reset the device.

---

## 3.7 Switch ON Resistance (R_ON)

| VCC range      | Test condition       | Min | Typ | Max | Unit |
| -------------- | -------------------- | --- | --- | --- | ---- |
| 4.5 V – 5.5 V  | VO = 0.4 V, IO = 15 mA | 4   | 10  | 20  | Ω    |
| 3.0 V – 3.6 V  | VO = 0.4 V, IO = 15 mA | 5   | 12  | 30  | Ω    |
| 2.3 V – 2.7 V  | VO = 0.4 V, IO = 10 mA | 7   | 15  | 45  | Ω    |

---

## 3.8 Pass-Gate / Voltage Translation

The pass-gate transistors let VCC clamp the maximum voltage passed between buses, enabling level translation. For the device to act as a translator, the pass voltage (V_pass) must be **≤ the lowest bus voltage**.

* Example: main bus 5 V, downstream buses 3.3 V and 2.7 V → set VCC = 3.3 V so V_pass(max) ≈ 2.7 V clamps the downstream buses. External pull-ups then bring each bus to its target level.
* When voltage translation is **not** needed, tie VCC and all pull-up reference voltages to the same supply.

---

# 4. Absolute Maximum Ratings

| Parameter           | Min     | Max    |
| ------------------- | ------- | ------ |
| Supply Voltage      | -0.5 V  | 7 V    |
| Input Voltage       | -0.5 V  | 7 V    |
| Input Current       | -20 mA  | 20 mA  |
| Output Current      | -25 mA  | 25 mA  |
| Supply Current      | -100 mA | 100 mA |
| Storage Temperature | -65°C   | 150°C  |

---

# 5. Physical Pins

## 5.1 24-Pin Package Pinout

| Pin | Name  | Direction | Function           |
| --- | ----- | --------- | ------------------ |
| 1   | A0    | Input     | I²C address select |
| 2   | A1    | Input     | I²C address select |
| 3   | RESET | Input     | Active-low reset   |
| 4   | SD0   | I/O       | Channel 0 data     |
| 5   | SC0   | I/O       | Channel 0 clock    |
| 6   | SD1   | I/O       | Channel 1 data     |
| 7   | SC1   | I/O       | Channel 1 clock    |
| 8   | SD2   | I/O       | Channel 2 data     |
| 9   | SC2   | I/O       | Channel 2 clock    |
| 10  | SD3   | I/O       | Channel 3 data     |
| 11  | SC3   | I/O       | Channel 3 clock    |
| 12  | GND   | —         | Ground             |
| 13  | SD4   | I/O       | Channel 4 data     |
| 14  | SC4   | I/O       | Channel 4 clock    |
| 15  | SD5   | I/O       | Channel 5 data     |
| 16  | SC5   | I/O       | Channel 5 clock    |
| 17  | SD6   | I/O       | Channel 6 data     |
| 18  | SC6   | I/O       | Channel 6 clock    |
| 19  | SD7   | I/O       | Channel 7 data     |
| 20  | SC7   | I/O       | Channel 7 clock    |
| 21  | A2    | Input     | I²C address select |
| 22  | SCL   | I/O       | Upstream clock     |
| 23  | SDA   | I/O       | Upstream data      |
| 24  | VCC   | —         | Supply voltage     |

All active I2C segments require pull-up resistors to their intended bus rail. For voltage translation, those rails may differ; follow the TI pass-voltage constraints instead of assuming every pull-up goes to device VCC.

---

# 6. Hardware Integration Constraints

## Pull-Up Resistors

Required on:

* SDA
* SCL
* SD0–SD7
* SC0–SC7

---

## Pull-Up Resistance Equations

### Minimum Pull-Up

```
Rp(min) = (VDPUX − VOL(max)) / IOL
```

---

### Maximum Pull-Up

```
Rp(max) = tr / (0.8473 × Cb)
```

Where:

| Parameter | Meaning         |
| --------- | --------------- |
| tr        | rise time       |
| Cb        | bus capacitance |

---

## Maximum Bus Capacitance

```
Cb ≤ 400 pF
```

---

# 7. Communication Interfaces

## Supported Interface

* I²C
* SMBus compatible

---

## I²C Clock Frequency

| Mode          | Frequency |
| ------------- | --------- |
| Standard Mode | 0–100 kHz |
| Fast Mode     | 0–400 kHz |

---

## Bus Requirements

Both lines must have pull-up resistors:

```
SCL
SDA
```

---

# 8. Addressing and Device Identification

## I²C Address Format

The 7-bit slave address has a fixed upper nibble `1110` and three hardware-selectable bits set by the A2/A1/A0 pins:

```
MSB                          LSB
 1   1   1   0   A2  A1  A0  | R/W
```

* R/W = 0 → write, R/W = 1 → read.
* A0–A2 must be tied directly to VCC or GND and must **not** change between START and STOP.

> Note: Figure 8-4 in the TI datasheet prints the bit labels as `A1 A2 A0`, but the authoritative Address Reference table (Table 8-1) maps A2/A1/A0 → 0x70–0x77 as shown below. Use the table ordering.

## Address Range

| A2 | A1 | A0 | 7-bit Address | Decimal |
| -- | -- | -- | ------------- | ------- |
| 0  | 0  | 0  | 0x70          | 112     |
| 0  | 0  | 1  | 0x71          | 113     |
| 0  | 1  | 0  | 0x72          | 114     |
| 0  | 1  | 1  | 0x73          | 115     |
| 1  | 0  | 0  | 0x74          | 116     |
| 1  | 0  | 1  | 0x75          | 117     |
| 1  | 1  | 0  | 0x76          | 118     |
| 1  | 1  | 1  | 0x77          | 119     |

---

# 9. Communication Protocol

The device exposes a **single 8-bit control register** with no sub-address/register pointer. A write sends the slave address then the control byte; a read returns the control byte directly.

## Write Transaction

```
START
DEVICE_ADDRESS + W
ACK
CONTROL_BYTE        (channel mask)
ACK
STOP
```

* The selected channel(s) become active **only after the STOP** condition.
* There is no limit on the number of bytes sent; if multiple bytes are written, the device **keeps the last byte received**.

## Read Transaction

```
START
DEVICE_ADDRESS + R
ACK
CONTROL_BYTE        (current channel mask, from device)
NACK (from master)
STOP
```

* A read returns the live value of the control register (the current channel mask). No command/pointer byte is required.
* The master signals end-of-data with a NACK on the final byte before STOP.

---

# 10. Register Map

The device contains **one register**.

| Register         | Address  | Access | Reset | Description            |
| ---------------- | -------- | ------ | ----- | ---------------------- |
| Control Register | implicit | R/W    | 0x00  | Channel enable control |

---

# 11. Register Bitfields

## Control Register

| Bit | Name            | Function    |
| --- | --------------- | ----------- |
| B0  | Channel0 Enable | 1 = enabled |
| B1  | Channel1 Enable | 1 = enabled |
| B2  | Channel2 Enable | 1 = enabled |
| B3  | Channel3 Enable | 1 = enabled |
| B4  | Channel4 Enable | 1 = enabled |
| B5  | Channel5 Enable | 1 = enabled |
| B6  | Channel6 Enable | 1 = enabled |
| B7  | Channel7 Enable | 1 = enabled |

Default value:

```
0x00
```

All channels disabled.

---

# 12. Reserved Bit Handling

No reserved bits exist in the control register.

---

# 13. Data Formats

### Control Register Format

```
MSB                              LSB
B7 B6 B5 B4 B3 B2 B1 B0
```

Each bit enables a channel.

---

# 14. Timing Requirements

## I²C Timing (Standard Mode)

| Parameter                          | Min  | Max  | Unit |
| ---------------------------------- | ---- | ---- | ---- |
| Clock frequency (f_scl)            | 0    | 100  | kHz  |
| Clock high (t_sch)                 | 4    |      | µs   |
| Clock low (t_scl)                  | 4.7  |      | µs   |
| Spike suppression (t_sp)           |      | 50   | ns   |
| Data setup (t_sds)                 | 250  |      | ns   |
| Data hold (t_sdh)                  | 0    |      | µs   |
| Input rise time (t_icr)            |      | 1000 | ns   |
| Input fall time (t_icf)            |      | 300  | ns   |
| Bus free, STOP→START (t_buf)       | 4.7  |      | µs   |
| START setup (t_sts)                | 4.7  |      | µs   |
| START hold (t_sth)                 | 4    |      | µs   |
| STOP setup (t_sps)                 | 4    |      | µs   |
| Bus capacitance (Cb)               |      | 400  | pF   |

## I²C Timing (Fast Mode)

| Parameter                          | Min          | Max  | Unit |
| ---------------------------------- | ------------ | ---- | ---- |
| Clock frequency (f_scl)            | 0            | 400  | kHz  |
| Clock high (t_sch)                 | 0.6          |      | µs   |
| Clock low (t_scl)                  | 1.3          |      | µs   |
| Spike suppression (t_sp)           |              | 50   | ns   |
| Data setup (t_sds)                 | 100          |      | ns   |
| Data hold (t_sdh)                  | 0            |      | µs   |
| Input rise time (t_icr)            | 20 + 0.1·Cb   | 300  | ns   |
| Input fall time (t_icf)            | 20 + 0.1·Cb   | 300  | ns   |
| Bus free, STOP→START (t_buf)       | 1.3          |      | µs   |
| START setup (t_sts)                | 0.6          |      | µs   |
| START hold (t_sth)                 | 0.6          |      | µs   |
| STOP setup (t_sps)                 | 0.6          |      | µs   |
| Bus capacitance (Cb)               |              | 400  | pF   |

(Cb = total bus capacitance of one line, in pF.)

## Reset Timing

| Parameter                          | Min | Max | Unit |
| ---------------------------------- | --- | --- | ---- |
| RESET low pulse width (t_W(L))     | 6   |     | ns   |
| Recovery, RESET→START (t_REC(STA)) | 0   |     | ns   |
| RESET→SDA clear (t_rst)            |     | 500 | ns   |

---

# 15. Reset Behavior

## Hardware Reset (RESET pin)

Signal:

```
RESET (active low)
```

* Asserting RESET low for at least `t_W(L) = 6 ns` resets the registers and the I²C state machine and deselects all channels.
* RESET must be connected to VCC through a pull-up resistor when not actively driven.
* Primary use: recover when a downstream I²C bus is stuck low, without cycling power.

### RESET Errata (important)

If the RESET pin is driven **higher than VCC**, current flows from RESET into VCC and pulls VCC above its normal level. **Always design so RESET voltage ≤ VCC.**

## Power-On Reset (POR)

* On power-up, an internal POR holds the device in reset until VCC reaches VPORR (rising). Registers and the I²C state machine then initialize to defaults (all channels deselected).
* For a deliberate power-reset cycle, VCC must be lowered **below VPORF** and then ramped back up above VPORR. A glitch that stays above VPORF does not reset the part.
* See §3.6 for VPORR / VPORF values.

## Reset Effects

Both RESET and POR cause:

* I²C state machine reset
* Control register cleared to `0x00`
* All channels deselected

## Reset Propagation

```
RESET → SDA clear delay ≈ 500 ns (max)
```

---

# 16. Operating Modes

## Modes

1. **Reset Mode**
2. **Idle Mode**
3. **Channel Active Mode**

---

## Default Mode

```
All channels disabled
```

---

# 17. Status and Diagnostics

No dedicated status register.

Diagnostics available via:

* reading control register

---

# 18. Interrupts

The device **does not implement interrupts**.

---

# 19. Measurement or Operation Flow

## Channel Selection Procedure

```
1 Send START
2 Send device address + write
3 Send control byte
4 STOP
5 Selected channel becomes active
```

---

# 20. Performance Characteristics

## Switch ON Resistance (R_ON)

| VCC range     | Min | Typ | Max | Unit |
| ------------- | --- | --- | --- | ---- |
| 4.5 V – 5.5 V | 4   | 10  | 20  | Ω    |
| 3.0 V – 3.6 V | 5   | 12  | 30  | Ω    |
| 2.3 V – 2.7 V | 7   | 15  | 45  | Ω    |

## Propagation Delay (t_pd)

The pass-gate propagation delay is the RC time constant of R_ON and the load capacitance:

| Condition              | Max  | Unit |
| ---------------------- | ---- | ---- |
| R_ON = 20 Ω, CL = 15 pF | 0.3  | ns   |
| R_ON = 20 Ω, CL = 50 pF | 1    | ns   |

---

# 21. Driver Initialization Sequence

```
1 Apply VCC
2 Wait for power-on reset completion
3 Confirm I²C communication
4 Write control register (0x00)
```

---

# 22. Runtime Operation Sequences

## Enable Single Channel

```
write register = 1 << channel
```

Example:

```
channel 3 → 0x08
```

---

## Enable Multiple Channels

```
register = bitmask
```

Example:

```
channels 0,2,4 → 0b00010101
```

---

# 23. Recommended Driver State Model

Driver should track:

```
current_channel_mask
i2c_address
bus_state
reset_state
```

---

# 24. Required Safety and Correctness Rules for Code Generation

1. Always issue STOP after writing control register
2. Channel activation occurs only after STOP
3. Respect I²C timing constraints
4. Ensure SDA stable during SCL high
5. Do not change A0–A2 during transaction
6. Always provide pull-ups
7. Reset channel state after reset
8. Ensure RESET voltage ≤ VCC
9. Bus capacitance must not exceed 400 pF

---

# 25. Minimal Functional Feature Set

Minimum driver must implement:

* I²C address configuration
* control register read/write
* channel enable/disable
* reset handling

---

# 26. Canonical Constants

```
PCA9548A_BASE_ADDRESS = 0x70

PCA9548A_CHANNEL0 = 0x01
PCA9548A_CHANNEL1 = 0x02
PCA9548A_CHANNEL2 = 0x04
PCA9548A_CHANNEL3 = 0x08
PCA9548A_CHANNEL4 = 0x10
PCA9548A_CHANNEL5 = 0x20
PCA9548A_CHANNEL6 = 0x40
PCA9548A_CHANNEL7 = 0x80
```

---

# 27. Bitfield Constants

```
PCA9548A_CH0_MASK = 0x01
PCA9548A_CH1_MASK = 0x02
PCA9548A_CH2_MASK = 0x04
PCA9548A_CH3_MASK = 0x08
PCA9548A_CH4_MASK = 0x10
PCA9548A_CH5_MASK = 0x20
PCA9548A_CH6_MASK = 0x40
PCA9548A_CH7_MASK = 0x80
```

---

# 28. Enumerations

```
enum PCA9548A_Channel
{
    PCA9548A_CHANNEL_0,
    PCA9548A_CHANNEL_1,
    PCA9548A_CHANNEL_2,
    PCA9548A_CHANNEL_3,
    PCA9548A_CHANNEL_4,
    PCA9548A_CHANNEL_5,
    PCA9548A_CHANNEL_6,
    PCA9548A_CHANNEL_7
};
```

---

# 29. Final Implementation Intent

A generated firmware driver should:

* Provide channel selection APIs
* Handle multiple channel enable masks
* Maintain correct I²C protocol timing
* Correctly reset device state
* Prevent invalid address changes during transactions

---

## Common Implementation Mistakes

1. Forgetting STOP after register write
2. Enabling channels before STOP
3. Exceeding I²C bus capacitance
4. Omitting pull-ups on downstream buses
5. Driving RESET above VCC
6. Not clearing channels after reset

---

# 30. Board Context: M5Stack Unit PaHub v2.0 (SKU U040-B)

This PCA9548A is the core IC of the **M5Stack Unit PaHub v2.0** used in this project. When writing a board-level driver, apply these board-specific constraints on top of the generic chip behavior above. (See `pahub.md` for the full board specification.)

## 30.1 Board Differences vs. Raw Chip

| Aspect            | Raw PCA9548A          | PaHub v2.0 board                              |
| ----------------- | --------------------- | --------------------------------------------- |
| Exposed channels  | 8 (channels 0\u20137)      | **6** (channels 0\u20135 routed to connectors J1\u2013J6) |
| Channels 6 & 7    | Usable (SC6/7, SD6/7) | **Not routed** \u2013 do not expose to callers     |
| Default address   | Strap-dependent       | **0x70** (A2=A1=A0=0 via 10 k\u03a9 pull-downs)    |
| RESET pin         | Host-controllable     | Pulled up on-board, **not** MCU-controllable  |
| Address change    | A0\u2013A2 pins            | Only by reworking 0 \u03a9 straps to VCC            |

## 30.2 Channel \u2192 Connector Map

| External Port | Connector | Channel | Control Byte   |
| ------------- | --------- | ------- | -------------- |
| Port 0        | J1        | 0       | 0x01           |
| Port 1        | J2        | 1       | 0x02           |
| Port 2        | J3        | 2       | 0x04           |
| Port 3        | J4        | 3       | 0x08           |
| Port 4        | J5        | 4       | 0x10           |
| Port 5        | J6        | 5       | 0x20           |
| (none)        | —         | —       | 0x00 (all off) |

## 30.3 Board-Level API Rules

* `pahub_select_channel()` must reject channel indices `> 5` (board-invalid channels 6 and 7).
* Default to **all channels off (0x00)** after init.
* The board is intended for **same-address device coexistence**: enable **exactly one channel at a time** before each downstream transaction sequence.
* `pahub_disable_all()` writes `0x00`.
* Keep bus speed \u2264 400 kHz and total bus capacitance \u2264 400 pF (longer Grove cables add capacitance).

## 30.4 Recovery (no host RESET line)

Because the RESET pin is not host-controllable on this board, recover from faults by:

1. Writing `0x00` to deselect all channels.
2. Retrying a single-channel selection.
3. Power-cycling the module supply if a downstream device is holding SDA low.
4. Re-probing addresses `0x70`\u2013`0x77` if the address straps were reworked.

## 30.5 Recommended Driver State

```c
struct pahub_state {
    uint8_t i2c_addr;       /* 0x70..0x77, default 0x70 */
    uint8_t selected_mask;  /* last written control byte; may desync after power cycle */
};
```

Treat the cached `selected_mask` as advisory \u2014 always tolerate desynchronization after a module power cycle by re-writing the desired channel.
